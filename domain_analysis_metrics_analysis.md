# domain_analysis 指标分析文档

本文档梳理 `domain_analysis.py` 中用于证明天气 Domain Shift 的指标：**分析了什么**、**如何定义**、以及**这些指标是否有意义（含局限）**。

---

## 1. `domain_analysis.py` 实际分析了哪些“指标”

严格来说，这个脚本包含两层指标：

1. **帧级特征（feature vector）**：先把每一帧点云压缩成统计向量；
2. **Domain 差异指标（distance / separability metrics）**：在这些帧级特征上评估不同天气域与 Clean 域的差异程度。

### 1.1 帧级特征（用于后续 PAD / t-SNE / 分类器）

每一帧会提取以下特征：

- 点数 `N = len(pts)`
- 强度统计：均值、标准差、中位数、偏度、峰度、10/90 分位数
- 距离统计：均值、标准差、中位数
- 距离直方图（`0~80m` 划分为 `num_dist_bins` 个 bin，默认 20，使用 density 归一化）
- 强度直方图（`0~1` 划分为 `num_int_bins` 个 bin，默认 20，使用 density 归一化）
- 近/远距离比例：`dist < 5m`、`<10m`、`<20m`，以及 `dist > 50m`、`>70m`
- `z` 轴统计：均值、标准差

> 这些不是最终“差异分数”，而是供后续分类、可视化和距离估计的输入表示。

### 1.2 Domain 差异核心指标

#### A) Proxy A-distance (PAD)

脚本通过线性 SVM 做二分类（source vs target），并用 5 折交叉验证准确率估计误差率：

- `error = 1 - acc`
- `PAD = 2 * (1 - 2*error)`

代码中将 PAD 截断到非负（`max(pad, 0)`），并给出经验解释区间：

- `PAD = 0`：两域几乎不可区分
- `PAD = 2`：两域几乎可完美区分

同时输出 **domain classifier accuracy**（分类准确率矩阵）。

#### B) t-SNE 2D 投影（可视化指标）

对标准化后的帧级特征先 PCA 到 50 维（可选），再 t-SNE 到 2 维，观察不同 domain 的聚类分离度。

> 这不是单一标量分数，而是“结构性证据”。

#### C) Radar Chart 综合指标（来自逐帧统计聚合）

`domain_analysis.py` 里雷达图用到的指标来自 `compute_statistics`（在 `vis_and_diff.py` 定义）：

- `intensity_wasserstein`
- `intensity_KL_divergence`
- `point_ratio`
- `near_point_ratio_weather`
- `far_point_retention`
- `MMD_xyz`
- `MMD_intensity`
- `intensity_KS_statistic`

并做了可视化变换：

- `point_ratio` 转换为 `abs(1 - point_ratio)`（偏离度）
- `far_point_retention` 转换为 `1 - far_point_retention`（远距丢失率）
- 再按人为设置的 `[vmin, vmax]` 线性归一化到 `[0,1]`

---

## 2. 各指标定义、意义与局限

下面按“定义 → 代表什么 → 是否有意义（局限）”来评估。

### 2.1 PAD / Domain Classifier Accuracy

**定义**
- 训练线性分类器区分两域；若分类越容易，说明域差异越大。
- PAD 本质来自 domain adaptation 理论中的可分性上界思想。

**代表什么**
- 对“可学习可分性”的度量，比纯分布距离更贴近下游任务风险（因为它直接问：这两个域能不能被分类器分开）。

**有意义吗？**
- **有意义，且是主指标之一。** 特别适合论证“天气域与清洁域存在可检测的 shift”。

**局限**
- 依赖特征工程（你提取了什么统计量，就看到什么 shift）。
- 依赖分类器容量（这里只用线性 SVM，能反映线性可分性，但可能低估非线性差异）。
- 对样本量、类别平衡、随机划分有敏感性。

---

### 2.2 t-SNE 聚类分离

**定义**
- 非线性降维，把高维帧特征映射到 2D。

**代表什么**
- 不同域是否形成可视化上的簇分离。

**有意义吗？**
- **有辅助意义。** 很适合论文图示，直观表达“域在特征空间分开了”。

**局限**
- 不是严格可比较的数值统计；
- 对 perplexity、随机种子、样本规模敏感；
- 局部邻域可信，全球几何关系不一定可信。

> 结论：t-SNE 应作为“可视化佐证”，不能单独作为定量结论。

---

### 2.3 `point_ratio`（点数比例）

**定义**
- `len(weather_pts) / len(clean_pts)`。

**代表什么**
- 天气后总点数是增加（噪声/回波）还是减少（衰减/遮挡）。

**有意义吗？**
- **有意义，且解释性强。** 直接对应传感器退化现象。

**局限**
- 受场景几何变化影响（动态物体/裁剪范围）；
- 只反映“总量”，不反映“空间上丢在哪里”。

---

### 2.4 `near_point_ratio_weather`（近距点比例）

**定义**
- `np.sum(dist < 5) / len(weather_pts)`。

**代表什么**
- 近距离伪点/散射噪声倾向（特别在雨雪回波中常见）。

**有意义吗？**
- **有意义。** 对“天气引入近场噪声”很敏感。

**局限**
- `<5m` 阈值是经验设置，不同 LiDAR、安装位置需要调参。

---

### 2.5 `far_point_retention`（远距保留率）

**定义**
- `count(dist>50m in weather) / count(dist>50m in clean)`。

**代表什么**
- 远距点是否大量消失（雾/强雨常导致远处回波衰减）。

**有意义吗？**
- **非常有意义。** 与自动驾驶感知退化高度相关。

**局限**
- `50m` 阈值是经验值；
- 远距本身点就少时，统计波动会大。

---

### 2.6 强度分布类：KL / KS / Wasserstein

#### `intensity_KL_divergence`

**定义**
- 基于强度直方图离散分布的 KL 散度：`KL(P_clean || P_weather)`。

**意义**
- 衡量天气导致的反射强度分布形变。

**局限**
- 非对称；对 bin 选择敏感；
- 当某些 bin 概率很小，数值会受平滑项影响。

#### `intensity_KS_statistic`

**定义**
- 两样本 KS 统计量：两经验 CDF 的最大差。

**意义**
- 不依赖具体分布假设，检测整体分布差异。

**局限**
- 只看最大间隙位置，不能完整表达“差多少”。

#### `intensity_wasserstein`

**定义**
- Earth Mover 距离：把一个强度分布“搬运”到另一个分布的最小代价。

**意义**
- 兼顾“差异幅度”和“差异位置”，解释性通常比 KL 更稳定。

**局限**
- 只在一维强度上，忽略空间结构。

**总体判断**
- 三者互补，**有意义**。其中 Wasserstein + KS 的组合通常更稳健，KL 可做补充。

---

### 2.7 MMD (`MMD_xyz`, `MMD_intensity`)

**定义**
- 基于 RBF kernel 的 Maximum Mean Discrepancy：比较两个样本分布在 RKHS 中的均值差。

**代表什么**
- 非参数、可捕获非线性分布差异。

**有意义吗？**
- **有意义。** 适合作为“整体分布距离”补充 PAD。

**局限**
- 对核参数 `gamma`、采样策略敏感；
- 当前实现 `gamma = 1/d` 较粗糙，可能未对不同尺度做最优匹配。

---

## 3. 这些指标整体上“有意义吗”？

结论：**整体是有意义的，而且组合合理。**

- PAD/分类准确率：衡量可分性（任务相关）
- 分布距离（KL/KS/Wasserstein/MMD）：衡量统计差异（机制层）
- 几何退化（near/far/point_ratio）：衡量感知退化形态（工程解释层）
- t-SNE/雷达图：提升可解释可视化（展示层）

它们形成了“**可分性 + 分布差异 + 物理退化**”三层证据链，不是单一指标。

---

## 4. 需要注意的实现与方法学问题（重要）

1. **模块依赖一致性（已修复）**
   - 当前代码已改为 `from vis_and_diff import compute_statistics`，与仓库内函数定义一致。后续若重命名文件，需要同步更新导入路径。

2. **雷达图归一化上下限是手工设定**
   - `metrics_config` 的范围（如 `MMD` 上限 0.1）会影响“面积大小”；不同数据集可能需要重标定。

3. **未给出统计显著性与置信区间**
   - 当前主要输出均值和图；建议增加 bootstrap CI 或多次随机重复，避免偶然性。

4. **特征工程偏向“统计摘要”**
   - 能抓住全局 shift，但可能遗漏局部结构变化（如某些语义区域受天气影响更大）。

---

## 5. 建议（用于把“有意义”进一步做扎实）

- PAD 增补非线性分类器（如 RBF-SVM 或小型 MLP）并报告对比；
- MMD 使用 median heuristic 或多核 MMD，减少核参数敏感性；
- 对 near/far 阈值做敏感性分析（5m/50m 改为多阈值曲线）；
- 给每个指标报告跨帧置信区间，而非仅均值；
- 若用于论文主结论，增加“与检测性能下降相关性”分析（例如指标与 mAP 降幅相关系数）。

---

## 6. 一句话总结

`domain_analysis` 不是只算一个距离，而是在 **可分性（PAD/分类器）+ 分布距离（KL/KS/Wasserstein/MMD）+ 物理退化统计（点数、近噪声、远丢失）** 三个层面共同证明天气 domain shift；这些指标总体**有意义**，但需要注意阈值/核参数/可视化归一化带来的方法敏感性。
